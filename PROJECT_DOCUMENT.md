# PM Runtime v0.1 项目开发基线

设计日期：2026-09-22。面向开发项目经理与全栈开发 Agent。本文将会议记录到可编辑图形的闭环转成实施范围、任务和验收依据；本次仅交付设计与协议资料，不包含业务实现。

## 1 依据与阅读顺序

唯一产品基线为 [PM Runtime 产品与 MVP 技术方案 v0.1](PM_Runtime_产品与MVP技术方案_v0.1.docx)，版本日期 2026-09-21。已读取全文、表格及两张架构/流程图。当前目录没有应用源码、package.json、数据库、UI 原型或既有工程约定。

按以下顺序阅读：本文件 → [ARCHITECTURE.md](ARCHITECTURE.md) → [DATABASE_PLAN.md](DATABASE_PLAN.md) → [API_CONTRACT.md](API_CONTRACT.md) → [任务索引](docs/tasks/README.md) → [TASK_BOARD.md](TASK_BOARD.md) → [COMMUNICATION.md](COMMUNICATION.md)。字段协议见 [DSL Schema](docs/contracts/diagram.schema.json)，技术出处见 [官方资料](docs/references/TECH_REFERENCES.md)。文档中的目录和 npm 命令是开发目标，除明确注明的环境探测外，尚未实现或运行。

标记含义：**需求**来自源文档；**设计决策**是本次确定的实现约定；**待确认**是缺失的业务或发布输入。设计决策可供首期实施，但不应被描述为已验证的产品能力。后续需求调整先更新本基线及相关契约，再修改任务。

## 2 当前范围

v0.1 为 Windows 优先、单机单用户的本地工作层。用户在桌面端保存项目和会议，在已有宿主 Agent 中发出画图指令，再回到桌面端编辑、保存和导出。Runtime 不内置聊天、不调用模型 API、不持有模型密钥。

必须实现：项目新建/重命名/切换；粘贴与 TXT/MD/DOCX 导入；至少 5 万中文字符的完整分块读取；标准流程图与横向/纵向泳道图；字段与业务校验；draw.io 编辑；不可变版本；drawio/SVG/PNG 导出；重启恢复；Codex CLI 和 Claude Code 两个宿主真实联调。

暂不实现：录音、转写、PDF/OCR、PRD、排期、待办、风险与变更实体、向量检索、团队权限、同步、云服务、插件商店、Hooks、自动更新。Capture 只保留文档级扩展边界，不建空服务。DSH 保留通用 stdio 配置说明，其具体版本和接入形式待确认，不作为替代两个已选宿主的验收捷径。FTS5 无对应 P0 检索需求，本期不建索引。

安装包属于 v0.1 最终交付，在闭环通过后做一次 Windows 手工打包验收；不作为首次启动的前置工作。macOS 只隔离路径和进程适配，不承诺本期安装包。

## 3 业务链路与页面

1. 启动桌面端，Runtime 就绪；创建项目并成为当前项目。
2. Meetings 页粘贴或选择文件。解析预览确认后提交；成功才出现会议记录及 meeting_id。失败保留输入和原因。
3. 用户在 Agent 中指定项目、会议及目标流程。Agent 先列项目、再列会议、逐块读取；不能靠桌面“当前项目”的全局状态猜测目标。
4. Agent 根据 meeting-to-diagram 工作流输出 DSL，Runtime 校验、布局、生成 XML并提交第一版。错误返回字段位置；没有依据的分支向用户询问。
5. Diagrams 列表自动刷新。用户打开画布修改并显式保存，新 revision 持久化后才显示“已保存”。
6. 用户选择已保存版本，导出三种格式；重新启动后能打开相同会议、图形与历史版本。

没有 UI 原型，以下是功能布局约束，不是视觉定稿。一级入口最多四个，编辑器与版本详情使用 Diagrams 的子视图。

| 页面 | 内容与操作 | 关键反馈 |
| --- | --- | --- |
| Projects | 列表、新建、重命名、切换 | 空态引导；名称非空；同名允许并显示 ID 区分 |
| Meetings | 当前项目会议、导入/粘贴、原文、复制 ID | 预览解析结果；长文分块加载；不静默截断 |
| Diagrams | 列表、来源、编辑器、历史版本、导出 | 显示版本、未保存、来源映射状态及错误 |
| Connections / Settings | Runtime 状态、配置、探测、数据目录 | 区分“已配置”“探测成功”“近期调用”，不凭配置宣称已连接 |

**待确认 Q01：**源文档 §9 写“删除/版本”，但没有删除范围、保留期和恢复规则。v0.1 先实现明确要求的版本；不显示可用删除按钮、不增加删除 API。项目/会议编辑与删除也没有完整业务定义，暂不扩展为全套 CRUD。此项不阻塞生成闭环，但发布范围评审必须明确是否接受推迟图形删除。

## 4 用户与访问规则

本期只有本机用户，不做账号和 RBAC。桌面 UI 可创建/重命名项目、导入会议、读数据、编辑保存及导出。Agent 只能通过七个 MCP 工具读项目/会议和生成/修订 DSL；不能导入任意磁盘文件、删除数据、改设置或提交任意 XML。编辑器只能读写当前打开的图，不能直接访问文件系统或数据库。

所有操作按显式 project_id 校验关联关系；会议/图形存在但不属于项目时返回 project_mismatch。此校验用于避免跨项目误操作，不构成同一 OS 用户下的保密隔离。用户授权宿主连接后，宿主能读取本机用户的项目；更细粒度项目授权不在本期。Local First 指 Runtime 不主动向云端上传；宿主模型如何处理读取内容取决于宿主自身设置。

## 5 原文冲突和关键决策

| 项目 | 依据与处理 |
| --- | --- |
| 判断节点只有一条出边的示例 | §6 示例与 §5、A06 冲突。以验收为准：decision 至少两条带条件出边，缺失时 error。保留示例为负例，不能作为合法样例 |
| 工具命名不一致 | §4 的 get_recent_meeting/get_meeting 是概念描述。统一为 §7 七个工具；最近会议用 pm.meeting.list 显式排序与 limit=1 |
| 人工编辑后 DSL 失真 | 人工 XML 是该版本权威可编辑内容；原 DSL 仅为生成依据。不得假装同步。Agent 改图遇到人工版先返回冲突，默认另生成新图 |
| 文本来源却使用时间戳 | 没有转写时间轴。sourceRefs 采用规范化文本的 Unicode 码点半开区间；不编造时间范围。legacy quoteRange 不进入本契约 |
| 自动刷新 | 桌面每 2 秒刷新当前项目产物摘要，窗口重新聚焦立即刷新；无需 WebSocket 或消息队列 |
| 数据写入 | 单 Runtime 持有 SQLite；宿主各自启动 stdio 桥接进程。图形文件先完成、数据库后提交，避免成功记录指向半文件 |

## 6 需求到验收映射

ID 是本设计新增的追踪编号，需求内容均可回到原文；技术可靠性任务标为派生约束。

| 需求及来源 | 流程/页面 | 服务操作或 MCP | 数据 | 任务 | 验收 |
| --- | --- | --- | --- | --- | --- |
| R01 项目管理 §3.2/9 | 建项目、切换 / Projects | project.create/rename/list/get | projects、settings | TASK-001、002 | A01：重启仍存在，切换不混数据 |
| R02 会议输入 §3.2/12 | 导入 / Meetings | meeting.preview_import/create/get | meetings、源文件与文本 | TASK-003 | A02：5 万汉字和非 BMP 字符完整保存 |
| R03 Agent 读取 §7/12 | 指定会议 / 宿主 | pm.project.*、pm.meeting.* | projects、meetings | TASK-004 | A03：两个宿主读同一 ID 与文本哈希 |
| R04 语义与校验 §5–7 | 理解、修复 / 宿主 | pm.diagram.render | DSL、sourceRefs | TASK-005、009 | A06/07：缺分支拒绝，修复后成功 |
| R05 流程图 §3.2/11 | 出图 / Diagrams | pm.diagram.render/get | diagrams、revisions、文件 | TASK-005 | A04：真实会议生成可编辑图 |
| R06 泳道图 §5.2/11 | 职责交接 / Diagrams | 同上 | 同上 | TASK-006 | A05：3–6 泳道、10–30 节点，两种方向 |
| R07 编辑及版本 §9.1/13 | 改图、保存 / Diagrams | diagram.save_xml、pm.diagram.save_revision | revisions | TASK-007 | A08：改文字/节点/连线/位置均可重开 |
| R08 导出 §3.2/14 | 选版本导出 / Diagrams | diagram.get_revision + 本地导出桥 | 已保存 XML、导出文件 | TASK-008 | A09：三种格式可打开、中文可读 |
| R09 持久化 §8/14 | 保存与重启 / 全局 | 事务与文件提交 | SQLite、不可变文件 | TASK-002、007、010 | A10：重启与进程中断恢复 |
| R10 接入及交付 §2/13/20 | 配置、安装 / Settings | stdio、runtime.status | 配置/日志 | TASK-001、009、010 | 两宿主联调，干净用户目录安装 |

## 7 阶段与交接条件

**M0 技术风险验证：TASK-001。** 离线 draw.io 能加载、编辑、保存并导出中文；Node 固定运行时与 SQLite可持久化；stdio 能初始化。任一失败，记录原因并调整对应适配，不宣称完成产品。

**M1 第一条真实链路：TASK-002 → 003 → 004 → 005。** 桌面保存会议，宿主读取并真实推理，生成流程图落盘，桌面可打开。固定 DSL 仅验证渲染器，不能替代真实 Agent 链路。

**M2 完整 P0：TASK-006 → 007 → 008 → 009。** 泳道、编辑、版本、导出、双宿主流程工作流。**M3 发布验收：TASK-010。** 10 份脱敏真实会议 + 人工参考流程、异常与安装验收。

默认串行推进，不分配开发 Agent，不建立并行调度体系。每项任务完成时更新已运行命令、截图/文件、剩余问题；架构文档中的“设计完成”不等于代码验收通过。原文 32–42 人天仅是原估算，本次不据此承诺工期；M0 结果与真实语料齐备后由项目经理更新。

## 8 待确认与当前交付状态

| 编号 | 缺少的输入 | 阻塞范围 | 当前处理 |
| --- | --- | --- | --- |
| Q01 | 图形删除与保留规则 | 删除实现、最终范围签收 | 不实现删除；其余任务可执行 |
| Q02 | 10 份获准使用的真实/脱敏会议及产品参考流程 | TASK-010 语义质量验收 | 附录 B 只供开发冒烟，不能伪装十份真实样本 |
| Q03 | 实际 Codex CLI / Claude Code 版本、可用账户和运行权限 | TASK-009 双宿主真联调 | SDK 测试先行；不购买或假定账号可用 |
| Q04 | 发布 Windows 版本/架构及签名证书 | 正式分发环境承诺 | 先在开发机 Windows x64 验证，其他组合待定 |

已验证：本地 Node v22.23.2、npm 10.9.8、Git 2.46.0.windows.1 可执行；node:sqlite 内存查询成功，SQLite 3.51.3，并出现 ExperimentalWarning。尚未验证 Electron、draw.io 离线嵌入、布局、宿主连接或安装包。Schema/SQL/文档的检查记录见 [设计校验记录](docs/references/DESIGN_VALIDATION.md)。
